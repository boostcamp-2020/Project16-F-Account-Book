import styled from 'styled-components';

const MainContainer = styled.div`
  width: 100%;
  max-width: 768px;
  height: 100%;
  margin: 0 auto;
  padding: calc(51px + 1rem) 1rem 0;
`;

export default MainContainer;
