import styled from 'styled-components';

const name = styled.li`
  margin: 13px 8px;
  white-space: nowarp;
  font-weight: bold;
  font-size: 18px;
  word-break: keep-all;
  list-style: none;
`;

export default name;
