import styled from 'styled-components';
import defaultStyle from './default';

const NaverLoginButtonStyle = styled(defaultStyle)`
  background: #1ec800;
  background-image: url('/socials/naver.png');
  color: #fff;
`;

export default NaverLoginButtonStyle;
