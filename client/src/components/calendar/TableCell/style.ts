import styled from 'styled-components';

const TotalIn = styled.div`
  text-align: center;
`;

const TotalOut = styled.div`
  text-align: center;
  padding-left: 3px;
`;

export { TotalIn, TotalOut };
